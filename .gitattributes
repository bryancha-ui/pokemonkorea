*.mp4 filter=lfs diff=lfs merge=lfs -text
*.wav filter=lfs diff=lfs merge=lfs -text
*.m4a filter=lfs diff=lfs merge=lfs -text

# GitHub Pages must receive the actual Hwanung entrance movie. Git LFS pointer
# files are otherwise published verbatim and cannot be decoded by the browser.
phaser-game/public/assets/cutscenes/hwanung_entrance_final.mp4 -filter -diff -merge -text
